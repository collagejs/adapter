# Changesets

This repository uses Changesets for versioning and publishing packages from the monorepo root.

Use the following commands:

- `npm run changeset` to create a changeset
- `npm run version` to bump versions and update changelogs
- `npm run release` to publish the released packages
